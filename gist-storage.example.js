// Beispiel-Datei: gist-storage.example.js
// Kopiere diese Datei als `gist-storage.js` und trage dort deinen Token ein.
// Diese Datei wird committet und dient als Template; die echte `gist-storage.js`
// sollte in `.gitignore` stehen und nie in das öffentliche Repo gelangen.

// Ersetze 'DEIN_GITHUB_TOKEN_HIER' mit deinem Token (nur lokal, nicht committen)
const GITHUB_TOKEN = 'DEIN_GITHUB_TOKEN_HIER';

// Optional: Du kannst die restlichen Einstellungen aus deiner originalen
// `gist-storage.js` hierher kopieren, oder die Datei nur als Platzhalter nutzen.
